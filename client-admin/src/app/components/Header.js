import React, { PureComponent } from "react";
import "./Headers.scss";

class Header extends PureComponent {
  constructor(props) {
    super(props);
    this.state = {};
  }

  render() {
    return (
      <header>
        Lead Page App
      </header>
    );
  }
}

export default Header;
